import TestForm from "@/components/forms/TestiForm";
import React from "react";

const page = () => {
  return (
    <div className="flex items-center justify-center h-screen w-screen">
      <TestForm />
    </div>
  );
};

export default page;
